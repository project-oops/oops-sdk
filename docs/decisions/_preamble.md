# Decisions

Numbered, with reasoning, as they are made. The reasoning is the point - it is what stops a
choice being re-litigated by somebody who only has the choice.

**Nothing is recorded here yet.** The repository exists, it builds, and obSCEne links it - but
the choices behind it (why a separate repository rather than a directory inside its first
consumer, why platform entry points are weak declarations, what the display backend split is
answering) were made before this log did, and they belong to whoever made them. An entry
written by somebody reconstructing the reasoning afterwards is a guess with a number on it,
which is worse than an empty log.
